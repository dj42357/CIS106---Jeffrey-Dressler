#inputs
amount = float(input("Input the dollar amount recieved: "))

#processes
split = amount / 3

#outputs
print("")
#Same thing here, makes more sense to be happening with division
print("Split amount:",round(split,2))